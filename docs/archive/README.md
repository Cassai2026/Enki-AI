# Archive

This folder stores legacy narrative and research artifacts that are intentionally kept out of the repository root.

- `generated/` is used by report/manifest helper scripts for generated text outputs.
- Top-level legacy writeups were moved here to keep the main repository surface focused on product code and developer docs.
