"""
enki_ai.agents — real-time AI agent layer.

Contains the FastAPI/Socket.IO server (server.py) and all agent modules
(ada, cad_agent, web_agent, kasa_agent, printer_agent, authenticator, etc.).
"""
